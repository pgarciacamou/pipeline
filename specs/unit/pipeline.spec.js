const pipe = require("../../src/pipeline.js");
const pipeline = pipe.pipeline;
const {
  stop,
  skip
} = pipe.commands;
const {
  every,
  buffer,
  latest,
  log,
  execute
} = pipe.helpers;

describe('pipeline unit tests', function() {

});